import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  selectRange: {
    id: 'selectRange',
    description: 'Select date range title',
    defaultMessage: 'Select date range'
  }
});
